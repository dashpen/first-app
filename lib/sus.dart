import 'package:flutter/material.dart';

class Amogus extends StatelessWidget {
  const Amogus({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text("sus"));
  }
}